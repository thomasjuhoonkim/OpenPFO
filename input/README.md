To install cadQuery using pip:

python3 -m pip install --upgrade pip

pip install cadquery

to install from git: pip install git+https://github.com/CadQuery/cadquery.git

if using IPython: pip install cadquery[ipython]